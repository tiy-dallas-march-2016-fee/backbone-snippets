if (window.CollectionApp === undefined) { window.CollectionApp = {}; }

(function(context) {

  var FoodModel = Backbone.Model.extend({

  });

  context.FoodModel = FoodModel;

})(window.CollectionApp);
